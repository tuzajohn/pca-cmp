@model PCA.Web.Models.FolderCreateViewModel
@{
    ViewData["Title"] = "New Folder";
    var folders = ViewBag.Folders as List<PCA.Modules.Documents.Models.DocumentFolder> ?? new();
}

<div class="page-header">
    <div>
        <h1 class="page-header-title">New Folder</h1>
        <p class="page-header-sub mb-0">Create a folder to organise documents</p>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left me-1"></i> Cancel
    </a>
</div>

<div class="row">
    <div class="col-lg-6">
        <div class="form-section">
            <div class="form-section-header"><i class="bi bi-folder-plus"></i> Folder Details</div>
            <div class="form-section-body">
                <form asp-action="CreateFolder" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded-3 mb-4 small"></div>
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-semibold">Name <span class="text-danger">*</span></label>
                        <input asp-for="Name" class="form-control" placeholder="e.g. HR Policies" />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Description</label>
                        <input asp-for="Description" class="form-control" placeholder="Optional description" />
                    </div>
                    <div class="mb-4">
                        <label asp-for="ParentId" class="form-label">Parent Folder</label>
                        <select asp-for="ParentId" class="form-select">
                            <option value="">— Root level —</option>
                            @foreach (var f in folders.Where(x => x.ParentId == null).OrderBy(x => x.Name))
                            {
                                if (Model.ParentId == f.Id)
                                {
                                    <option value="@f.Id" selected>@f.Name</option>
                                }
                                else
                                {
                                    <option value="@f.Id">@f.Name</option>
                                }
                            }
                        </select>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-folder-plus me-1"></i> Create Folder
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<partial name="_ValidationScriptsPartial" />
}
